build.type = test
swf.debug = false

wrapper.template.dir = ${basedir}/html-templates
wrapper.template.html = ${build.type}.template.html
#Application file for Tests

(output)swf.name = ${project.name.short}
(output)swf = ${(output)wrapper.dir}/${(output)swf.name}.swf
(output)wrapper.html = index.html
(output)wrapper.url =  ${project.base.url}/deploy/${build.type}/${(output)wrapper.html}
(output)wrapper.dir = ${output.dir}/${build.type}
#Directory to copy deploy assets to
(output)wrapper.assets.runtime.dir = ${output.dir}/${build.type}/runtime
#HTML template file to use for the container when testing


