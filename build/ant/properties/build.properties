#Project Structure
src.dir = ${basedir}/src
libs.dir = ${basedir}/libs
test.dir = ${basedir}/test
bin.dir=${basedir}/bin
build.dir=${basedir}/build
output.dir = ${basedir}/deploy
assets.dir = ${basedir}/assets
assets.runtime.dir = ${assets.dir}/runtime
assets.embedded.dir = ${assets.dir}/embedded

#Distribution Structure
(output)dist.dir=${basedir}/dist
(output)dist.src.dir=${(output)dist.dir}/src
(output)dist.bin.dir=${(output)dist.dir}/bin
(output)dist.docs.dir=${(output)dist.dir}/docs
(output)dist.test.dir=${(output)dist.dir}/test
(output)dist.swc=${(output)dist.bin.dir}/{project.name.short}.swc
(output)dist.test.src.dir=${(output)dist.test.dir}/src
(output)dist.test.deploy.dir=${(output)dist.test.dir}/deploy
(output)dist.zip=${(output)dist.dir}/${project.name.short}.zip
application.file = ${application.name}.mxml






